doctype html
html[lang="en"]
  head
  body
    - if @invitation.user
      p
        = @invitation.user.full_name
        | ,
    p
      | You've been invited to join a project as a designer,  
      = @project.name
      |  on Bento.
    p
      = link_to "Click here", root_url + "/accept_designer_invitation/" + @project.id.to_s + "/" + @invitation.id.to_s
      |  to accept their invitation.
    p
      | -Bento
